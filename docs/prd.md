你好，请作为一名前端开发专家，帮我使用 HTML、Tailwind CSS (通过 CDN 引入) 和 Vanilla JavaScript 编写一个单页面应用（SPA）。
这个应用叫“早读动物园”，它的核心逻辑是通过检测麦克风音量（分贝），当音量超过阈值时，在屏幕上随机生成小动物。目前我只需要你帮我搭建高保真的静态 UI 界面以及基础的面板交互（不涉及真实的麦克风 API，用假数据模拟即可）。
请根据以下详细的模块拆解来编写代码：
一、 全局样式设计 (Global Styles)
整体风格：偏向儿童教育、活泼、可爱、干净。
背景色：非常浅的灰蓝色或纯白。
卡片样式：圆角 (rounded-xl 或 rounded-2xl)，带有柔和的阴影 (shadow-md 或 shadow-lg)，背景纯白。
二、 主界面布局 (Main Interface)
顶部标题区：
主标题：“早读动物园” (深蓝色，字体较大，加粗)。
副标题：“大声朗读小动物们才会出现，快点大声朗读吧！” (浅灰色，字体较小)。
动物展示区 (Canvas Container)：
这是一个占据屏幕大部分空间的大卡片。
背景：上半部分是浅蓝色（天空），最底部有大约 10%-15% 高度的浅绿色渐变色块（草地）。
请在这个容器的 CSS 中设置为 position: relative; overflow: hidden;。
占位：请用 JavaScript 在这个容器内随机生成 20 个左右的 Emoji（比如 🐰, 🐯, 🐷, 🐸, 🐶 等）作为绝对定位 (absolute) 的元素，随机散布在区域内，模拟图片中小动物乱跑的效果。
底部控制面板区 (Bottom Controls)：
包含三个并排的白色圆角卡片，高度一致。
左侧卡片（分贝仪）：左上角文字“当前分贝”，右上角带一个小喇叭图标。中间显示巨大的蓝色数字“35 dB”。底部有一行文字“分贝阈值 10 dB”以及一个水平的范围滑块 (range input)。
中间卡片（计时器）：左上角文字“大声朗读时间”，右上角带一个秒表图标。正中间显示巨大的绿色数字“00:01:50”。
右侧卡片（操作按钮）：纵向排列两个按钮。上面的按钮是“⚙️ 设置”，白底蓝边蓝字；下面的按钮是“⏸️ 暂停监测”，红粉色渐变背景，白字。
三、 设置弹窗面板 (Settings Modal - 图2内容)
这是一个隐藏的遮罩层 (Overlay)，点击主界面的“设置”按钮后显示。
弹窗标题：“动物园设置”。
动物生成速度区块：
小标题：“动物生成速度”。
包含一个范围滑块 (range input)。滑块上方有三个刻度标签：左侧“最快 (1秒)”，中间“1秒生成一只”，右侧“最慢 (60秒)”。
下方有一行小字提示：“调节动物在噪音环境中的生成频率”。
选择动物种类区块：
小标题：“选择动物种类”。
包含两个小按钮：“全选”和“全不选”。
下方是一个带边框和滚动条的网格区域 (CSS Grid)。请在里面放入大约 20 个带有复选框 (checkbox) 的 Emoji。每个 Item 是上面一个蓝色的 checkbox，下面对应一个动物 Emoji。
底部有一行小字提示：“选择你希望出现的动物种类（至少选择一种）”。
底部操作区：
两个并排按钮。左侧是“重置默认”（浅灰色背景），右侧是“保存设置”（蓝色背景，点击后关闭弹窗）。
(请注意忽略图片上原本那些巨大的粉红色标注文字，那不是UI)。
四、 JavaScript 基础交互
实现中间计时器数字的每秒递增。
实现“当前分贝”蓝色数字的随机小幅度跳动（比如在 30 到 45 之间随机变化），模拟真实的声音检测。
实现点击主界面的“设置”按钮，打开设置 Modal 弹窗；点击弹窗的“保存”或遮罩层外部，关闭弹窗。
实现弹窗内部“选择动物”区域的“全选 / 全不选”按钮的联动逻辑。
请将 HTML, CSS, JS 写在一个文件里提供给我，或者清晰地分开。使用高质量、语义化的代码。